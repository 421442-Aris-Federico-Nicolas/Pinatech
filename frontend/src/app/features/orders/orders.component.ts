import { DatePipe, DecimalPipe } from '@angular/common'; import { ChangeDetectionStrategy, Component, inject, signal } from '@angular/core'; import { MatCardModule } from '@angular/material/card'; import { OrderService, Order } from '../../core/orders/order.service';
@Component({imports:[DatePipe,DecimalPipe,MatCardModule],template:`<main><h1>Mis pedidos</h1>@for(order of orders();track order.id){<mat-card><mat-card-header><mat-card-title>Pedido #{{order.id}}</mat-card-title><mat-card-subtitle>{{order.createdAt|date:'medium'}} · {{order.status}}</mat-card-subtitle></mat-card-header><mat-card-content>@for(item of order.items;track item.productName){<p>{{item.quantity}} × {{item.productName}} <span>{{item.subtotal|number:'1.0-0'}} ARS</span></p>}<strong>Total: {{order.total|number:'1.0-0'}} ARS</strong></mat-card-content></mat-card>}@empty{<p>No tenés pedidos todavía.</p>}</main>`,styles:['main{margin:auto;max-width:800px;padding:3rem 1.5rem}mat-card{margin:1rem 0}p{display:flex;justify-content:space-between}strong{color:#047857}'],changeDetection:ChangeDetectionStrategy.OnPush}) export class OrdersComponent {private readonly service=inject(OrderService);readonly orders=signal<Order[]>([]);constructor(){this.service.mine().subscribe(orders=>this.orders.set(orders));}}
