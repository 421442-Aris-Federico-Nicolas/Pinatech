import { HttpClient } from '@angular/common/http'; import { Injectable, computed, inject, signal } from '@angular/core'; import { environment } from '../../../environments/environment'; import { Product } from '../../features/catalog/catalog.service';
export interface CartItem { product: Product; quantity: number; }
@Injectable({providedIn:'root'}) export class CartService { private readonly http=inject(HttpClient); private readonly itemsState=signal<CartItem[]>(this.restore()); readonly items=this.itemsState.asReadonly(); readonly count=computed(()=>this.itemsState().reduce((total,item)=>total+item.quantity,0)); readonly total=computed(()=>this.itemsState().reduce((total,item)=>total+item.product.price*item.quantity,0)); add(product:Product){const current=this.itemsState();const found=current.find(item=>item.product.id===product.id);this.update(found?current.map(item=>item===found?{...item,quantity:item.quantity+1}:item):[...current,{product,quantity:1}]);} setQuantity(id:number,quantity:number){this.update(this.itemsState().flatMap(item=>item.product.id===id&&quantity<=0?[]:item.product.id===id?[{...item,quantity}]:[item]));} clear(){this.update([]);} checkout(){return this.http.post(`${environment.apiBaseUrl}/orders`,{items:this.itemsState().map(item=>({productId:item.product.id,quantity:item.quantity}))});} private update(items:CartItem[]){this.itemsState.set(items);localStorage.setItem('computer-store-cart',JSON.stringify(items));} private restore():CartItem[]{try{return JSON.parse(localStorage.getItem('computer-store-cart')??'[]');}catch{return [];}} }
